<% if user_signed_in? && current_user.admin == false%>
    <h1><font color="#000000">Lo sentimos pero esta pantalla es sólo para administradores</h1>
<% elsif user_signed_in? && current_user.admin  %>
<div class="row justify-content-center">
    <div class="col-lg-6">
        <div class="login-content card">

            <h1>Editar media</h1>
                <%= render 'form', medium: @medium %>
                 <div class="row justify-content-center">
                   <div class="col-lg-6">
                      <div class="login-content card">
                        <%= link_to 'Atras', media_path ,{:style=>'color:#FFFFFF;', :class => "btn btn-primary btn-lg"}%>
                     </div>
                   </div>
                </div>
        </div>
    </div>
</div>
            <% end %>
